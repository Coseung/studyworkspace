<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="selectBoard">
		SELECT BOARD_NO,
		CATEGORY_NAME,
		BOARD_TITLE,
		BOARD_CONTENT,
		USER_ID,
		TO_CHAR(CREATE_DATE, 'YYYY/MM/DD') AS "CREATE_DATE"
		FROM BOARD
		LEFT JOIN CATEGORY USING(CATEGORY_NO)
		JOIN MEMBER ON(BOARD_WRITER = USER_NO)
		WHERE BOARD_NO = ?
	</entry>

	<entry key="selectList">
		SELECT BOARD_NO,
		CATEGORY_NAME,
		BOARD_TITLE,
		MEMBER_ID,
		COUNT,
		TO_CHAR(CREATE_DATE, 'YYYY/MM/DD') AS "CREATE_DATE"
		FROM BOARD B
		JOIN CATEGORY USING(CATEGORY_NO)
		JOIN MEMBER M ON(BOARD_WRITER = MEMBER_NO)
		WHERE B.STATUS = 'Y'
		AND BOARD_TYPE = 1
		ORDER BY CREATE_DATE DESC
	</entry>
	<entry key="selectListTest">
    SELECT BOARD_NO, CATEGORY_NO, BOARD_WRITER, BOARD_TITLE, STATUS, BOARD_TYPE
    FROM BOARD
	</entry>
	<entry key="selectCategoryList">
	    SELECT CATEGORY_NO,
		CATEGORY_NAME
		FROM CATEGORY
	</entry>
	<entry key="insertBoard">
	    INSERT 
		INTO BOARD(
					BOARD_NO,
					BOARD_TYPE,
					CATEGORY_NO,
					BOARD_TITLE,
					BOARD_CONTENT,
					BOARD_WRITER
					)
			VALUES(
					SEQ_BNO.NEXTVAL,
					?,
					?,
					?,
					?,
					?
			)
	</entry>
	<entry key ="selectBoard">
	SELECT BOARD_NO,
			   CATEGORY_NAME,
               BOARD_TITLE,
               BOARD_CONTENT,
               MEMBER_ID,
               TO_CHAR(CREATE_DATE, 'YYYY/MM/DD') AS "CREATE_DATE"
          FROM BOARD
          LEFT JOIN CATEGORY USING(CATEGORY_NO)
          JOIN MEMBER ON(BOARD_WRITER = MEMBER_NO)
         WHERE BOARD_NO = ?
	</entry>
	<entry key="updateBoard">
		UPDATE BOARD
		   SET BOARD_TYPE = ?,
		   	   CATEGORY_NO = ?,
		   	   BOARD_TITLE = ?,
		   	   BOARD_CONTENT = ?
		 WHERE BOARD_NO = ?
	</entry>
</properties>
